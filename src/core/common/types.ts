export type ChangePositionPayload = {
  id: number;
  position: number;
};

export type BaseUserInfo = {
  id: number;
  firstName: string;
  lastName: string;
  photoUrl: string;
  username: string;
};
